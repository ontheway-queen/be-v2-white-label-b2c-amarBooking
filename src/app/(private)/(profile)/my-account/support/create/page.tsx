import CreateSupport from '../_components/create-support';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      {' '}
      <CreateSupport />
    </div>
  );
};

export default page;
